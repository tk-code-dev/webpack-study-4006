extends layout

block content
  if user
    p Hello, #{user.username}
    a(href="/logout") Logout
  else
    a(href="/login") Login
  div#block.block
  button#scaling-button 拡大⬛縮小▪
  button#moving-button 移動💨
  div#root
  h1 サーバーステータス
  h3 ロードアベレージ
  p#loadavg
  script(src="/javascripts/bundle.js")
